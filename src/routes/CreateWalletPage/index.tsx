import CreateWallet from "@/components/blocks/CreateWallet";
import PageLayout from "@/components/layouts/PageLayout";

const CreateWalletPage: React.FC = () => {
  return (
    <PageLayout>
      <CreateWallet />
    </PageLayout>
  );
};

export default CreateWalletPage;
